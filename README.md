# car_parts_data_JSON
contains JSON file having data related to car parts with fields ( item number, item name, item image, item price). The data is scraped from a website using BeautifulSoup (PYTHON library for data scraping).
